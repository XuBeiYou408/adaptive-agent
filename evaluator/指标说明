 一、检索层 — 2 个指标

  1. Hit Rate @ 5（知识命中率）

  数据来源：golden_dataset.json 中每条测试数据有一个 expected_dad_id 字段，记录了"正确答案应该来自哪个文档父块"。评估时调用生产环境的
  zhaohui_and_rerank() 检索管道，得到 Top-5 文档列表。逐一比对每个文档的 dad_id 是否等于 expected_dad_id。

  计算方法：
  命中数 / 总测试条数 × 100%
  每条 case 只要 Top-5 中有任一文档的 dad_id 匹配，就算命中。

  为什么用：最直观的检索评估指标。它回答的问题是——"用户问了一个问题，我们能不能把他需要的那篇文档找出来？" Hit Rate
  低说明检索链路有问题（chunk 分割、embedding 质量、混合检索权重等）。用 @5 而非 @1 是因为 RAG 场景下会给 LLM 多个文档片段，前 5
  个内找到就算成功。

  2. MRR @ 5（平均倒数排名）

  数据来源：同上，基于每个 case 中匹配文档在 Top-5 列表中的排名位置。

  计算方法：
  每个 case: 1 / (命中排名)   未命中 = 0
  全部取平均
  例如：
  - 排第 1 命中 → MRR 贡献 1.0
  - 排第 3 命中 → MRR 贡献 0.33
  - 排第 5 命中 → MRR 贡献 0.20
  - 未命中 → MRR 贡献 0

  为什么用：Hit Rate 只看"有没有"，MRR 看"排在哪儿"。两个 case
  都命中了，一个排第一、一个排第五，对最终生成质量影响差别很大——排第五意味着前面 4 个文档压住了正确答案的位置。MRR
  能捕捉这种排序质量差异。

  ---
  二、工程层 — 2 个指标

  3. TTFT（Time To First Token，首字延迟）

  数据来源：评估开始时 t_start = time.time()，流式生成收到第一个 token 时 ttft_clock = time.time()。

  计算方法：
  TTFT = (首次收到token时间 - 发起查询时间) × 1000 ms
  全部 case 取平均

  为什么用：这是用户体验的核心指标。TTFT 包含了查询重写 + 向量检索 + BM25 检索 + 重排序 + LLM
  吐出第一个字的完整耗时。用户从输入问题到看到第一个字的时间决定了"系统快不快"的直观感受。4 秒和 2 秒的用户体验差别极大。

  4. End-to-End Latency（端到端延迟）

  数据来源：同上，用 t_end - t_start。

  计算方法：
  总延迟 = (流式生成完全结束时间 - 发起查询时间) × 1000 ms
  全部 case 取平均

  为什么用：衡量完整问答周期的耗时。TTFT 看首字，这个看全文时间。结合两者可以分析瓶颈在哪：TTFT 高 → 检索慢；TTFT 低但总延迟高 →
  生成慢（长篇回答）。

  ---
  三、生成层 — 3 个指标（LLM-as-Judge）

  5. Faithfulness（忠实度）

  数据来源：把当前 case 的「用户问题 + 检索到的上下文 + 系统生成的回答 + 参考答案」打包发给 DeepSeek，让它打分（0.0~1.0）。

  计算方法：LLM 阅读系统回答，逐句判断回答中的每一条陈述是否能在「检索到的上下文」中找到依据。找到依据 →
  高分，出现上下文里没有的内容（幻觉）→ 扣分。最后 20 条取平均。

  为什么用：这是 RAG 系统最重要的质量指标。RAG 的核心价值就是"基于知识库回答，不瞎编"。如果忠实度低，说明系统在编造信息，那 RAG
  就失去意义了。用 LLM 判断而非规则匹配，是因为自然语言的忠实度很难用 n-gram 重合度来衡量——一句话可以"措辞不同但语义忠实"。

  6. Answer Relevance（答案相关性）

  数据来源：同上，LLM 判断。

  计算方法：LLM 判断系统回答是否直接、精准地解答了用户提出的问题。回答切中要害 → 高分，答非所问或兜圈子 → 低分。

  为什么用：忠实度只保证"不瞎编"，不保证"回答了问题"。一个回答可以完全基于上下文但完全没回答问题——比如你问"怎么配置 A"，上下文确实有 A
  的文档，但系统回答的是 A 的背景介绍而非配置步骤。相关性衡量"答案是否对题"。

  7. Completeness（答案完整性）

  数据来源：同上，LLM 判断。

  计算方法：LLM 对比系统回答与「参考答案」（ground_truth），判断系统回答覆盖了几个关键信息点。覆盖全面 → 高分，遗漏重要信息 → 低分。

  为什么用：忠实度 + 相关性都好，但答案可能过于简略。比如 ground_truth 有 5 个要点，系统只说了 2
  个。完整性补充了这个维度，告诉你的系统是否给出了"足够充分"的回答。

  ---
  指标之间的关系

  检索质量          →    生成质量
  (Hit Rate, MRR)        (Faithfulness, Relevance, Completeness)
       ↓                          ↓
    决定了 LLM 能"看到"什么     决定了 LLM "回答"得怎么样

  工程性能 (TTFT, Latency) → 独立的体验维度，与上面两条线正交

  一个典型的问题诊断路径：
  - Hit Rate 65% + Relevance 0.57 → 检索没搜对，不是生成的问题
  - Hit Rate 90% + Faithfulness 0.4 → 检索搜对了，但 LLM 在瞎编
  - Faithfulness 0.83 + Completeness 0.43 → 不瞎编但回答太简略，需要改 prompt 引导更详细的输出
