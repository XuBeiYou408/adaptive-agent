# 项目里用了 Query Rewriting（查询重写），怎么合并多路查询结果的？遇到过什么问题吗？”
对于如何合并多路查询结果在我的rag项目中主要分为三个步骤
1.并发检索：系统并行驱动线程池对重写的问题集也就是原问题加两个重写问题，进行混合检索即向量检索召回和 bm25关键词检索
2.扁平化：对3个线程混合检索结果进行扁平化，合并过程中对每个数据注入来源标记方便后续对数据有疑问时进行排查
3.去重：对合并结果进行去重，对去重后的结果进行排序，返回给用户

在开发的时候我遇到过一个关于检索被截断和多路召回公平的的问题
在最开始的时候多路重写的召回的结果时二维的数组在扁平为一维数组的时候由于扁平化存在顺序偏序的问题即原问题排在最前面
最初因为性能的问题我随手做了一个暴力解法，即将多路结果进行排序，然后取前30条结果，
这个时候因为扁平化的排序问题导致重写检索召回的结果大部分都被原问题排挤出来
导致查询重写名存实亡了一般
发现这个问题后我删除了固定的截断即召回多少条结果就全部送到重排里面去
但是我也做了一个保底机制，即超过60条的结果全部删除
这样是因为我的本地模型开启了fp16的半精度推理模式，60条数据对于性能的影响不大

# 针对全链路异步化改造（高并发吞吐）如何陈述这个优化：
我的项目初始版本采用了同步阻塞架构。在进行压力测试时，
我发现当本地模型（Embedding/Reranker）进行矩阵计算时，会锁死 FastAPI 的单核主事件循环，导致接口吞吐量极低。
为此，我进行了全链路异步重构：
将 API 层改为 async def，利用 StreamingResponse 实现零阻塞的流式输出。
针对 LangChain 的 Runnable 链，我使用了 astream() 接口，
并通过 asyncio.to_thread 将底层计算密集型的模型推理逻辑（compute_score 和 encode）剥离到独立的 IO 线程池中。
这样改造后，FastAPI 的主线程只负责任务调度和 HTTP 响应发送，模型计算不再阻塞事件循环。
通过这种方式，我将系统的并发处理能力提升了数倍，确保了生产环境下的高可用性。”

# 为什么要在检索召回时使用线程池？
检索 (retrieve_single) 通常是同步 I/O 或 CPU 阻塞。
如果直接在 async 函数里调用，会阻塞事件循环。
放线程池里 → asyncio.to_thread 可以异步等待，
不阻塞其它请求。

#  项目中 Chunk Size 设了多大？怎么平衡检索精度和上下文完整性的？
最开始我设置的时chunk大小为300，但是后来发现语义检索召回率极高。
但喂给大模型时，由于缺乏前后文，大模型容易断章取义、回答片面。
后面设置了800，发现上下文极其丰富。但由于混杂了太多主题，向量特征被“稀释”了，导致检索精准度暴跌。
然后决定使用父子块联动的方法，在一开始数据清洗切分的时候选择切两个块，一个为父块一个为子块，
一开始在清洗完成后的数据中先切800字的夫块为每个父块添加上id，
然后在切好的父块中去切150字的子块， 然后为子块添加上父块的id的标志
这样在检索的时候向量特征非常聚焦，语义检索召回率极高然后在喂给大模型时将子块的内容拉伸为父块
最后再进行去重这样就能平衡精确度和上下文完整性

# 如果你的知识库文件发生变化（比如用户上传新文件、或者改了内容），你的 RAG 系统是怎么处理的？
由于FAISS只提供向量检索能力，不具备持久化和数据管理能力
所以每次重启时都需要把所有 PDF/MD 文件重新读取、重新切片、重新调用 Embedding 模型计算一遍
面对这种问题我将使用了一种轻量的同步机制，
即系统不会盲目加载，而是会在本地维护一个包含了每个文件绝对路劲，修改时间等特征的清单，
系统每次启动时都会先去访问清单，
然后对比清单中的文件是否被修改或者被删除了，
如果被修改删除了，则重新进行一遍持久化数据库的步骤并更新清单。因为FAISS 不支持高效更新和删除
如果只是新增了文件则就对新增的文件独立的进行一系列的读取清洗父子块的切分等操作然后直接加入本地的数据库
这样的话就不用每次都需要加载，大大节省了资源。